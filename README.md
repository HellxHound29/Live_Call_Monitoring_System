# Live_Call_Monitoring_System
Live Call Monitoring System built with Django (backend) and React + TypeScript (frontend). Enables real-time call tracking, user management, and call log monitoring via REST APIs. Features modular architecture, authentication, and a clean, production-ready project structure.
